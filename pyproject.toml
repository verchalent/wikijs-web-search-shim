[project]
name = "wikijs-web-search-shim"
version = "0.1.0"
description = "Add your description here"
authors = [
    { name = "Pestilent", email = "pestilent@amaterasu" }
]
requires-python = ">=3.14"
dependencies = []

[project.scripts]
wikijs-web-search-shim = "wikijs_web_search_shim:main"

[build-system]
requires = ["uv_build>=0.12.3,<0.13.0"]
build-backend = "uv_build"
